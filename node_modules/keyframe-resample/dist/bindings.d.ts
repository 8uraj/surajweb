import { Interpolation } from './constants.js';
export declare const ready: Promise<void>;
export declare function resample(input: Float32Array, output: Float32Array, interpolation: Interpolation, tolerance?: number): number;
